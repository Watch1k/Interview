
# 💼 Junior Frontend Developer Interview Questions (1–5)

> Відповіді на перші п’ять найпоширеніших питань для Junior Frontend Developer із практичними прикладами та поясненнями.

---

## 🔎 Загальні питання

### 1. Чи стежите ви за новинами у світі фронтенду? Як саме?

🟢 Так. Екосистема фронтенду розвивається дуже швидко, тому важливо бути в курсі нових технологій, фреймворків, специфікацій.

📌 **Інструменти/ресурси, які я використовую:**
- Поштові розсилки: [JavaScript Weekly](https://javascriptweekly.com), [Frontend Focus](https://frontendfoc.us)
- Блоги: CSS-Tricks, Smashing Magazine
- YouTube: Fireship, The Net Ninja
- Технічні подкасти: Syntax.fm, Shoptalk Show
- GitHub Trending — для нових інструментів
- Twitter/X — підписки на ключових експертів з React, TypeScript тощо

Це допомагає розуміти сучасні тренди, уникати застарілих практик і писати більш ефективний код.

---

### 2. Що відрізняє хороший код від поганого?

✅ **Хороший код:**
- легко читається іншими розробниками
- має логічну структуру та зрозумілі назви змінних
- використовує принципи SOLID, DRY, KISS
- містить юніт-тести (де потрібно)
- легко модифікується

❌ **Поганий код:**
- має дублювання
- не розбито на компоненти або функції
- не має очевидної логіки
- важко відлагоджувати
- відсутня документація

🔍 **Приклад:**
```js
// погано
let a = 10;
function x() { return a + 1; }

// добре
let userCount = 10;
function getNextUserId() { return userCount + 1; }
```

---

### 3. Які HTTP-методи та статуси знаєте?

📨 **HTTP-методи:**
- `GET` — отримати дані
- `POST` — створити нові дані
- `PUT` — оновити повністю
- `PATCH` — оновити частково
- `DELETE` — видалити
- `OPTIONS` — дізнатись, що дозволено

📦 **Коди статусів:**
- `200 OK` — все добре
- `201 Created` — успішно створено
- `204 No Content` — без відповіді
- `400 Bad Request` — неправильний запит
- `401 Unauthorized` — неавторизовано
- `403 Forbidden` — заборонено
- `404 Not Found` — не знайдено
- `500 Internal Server Error` — помилка сервера

---

### 4. У чому небезпека виконання коду, що надходить від користувача?

⚠️ Проблема полягає в тому, що користувач може передати шкідливий код (JavaScript, SQL тощо), який буде виконано у браузері або на сервері.

📌 **Можливі ризики:**
- XSS (Cross-Site Scripting) — вставка скриптів, які крадуть cookies або вводять користувача в оману
- SQL-інʼєкції (на бекенді)
- виконання шкідливих скриптів у iframe

✅ **Захист:**
- Валідація/ескейпінг введення
- Встановлення CSP (Content Security Policy)
- Не довіряти даним, навіть якщо вони приходять з "довіреного" джерела

---

### 5. Що таке ООП?

🧠 **ООП (обʼєктно-орієнтоване програмування)** — це парадигма програмування, яка базується на використанні **обʼєктів**, що обʼєднують дані (властивості) і поведінку (методи).

Основні **4 принципи ООП**:

1. **Інкапсуляція**
    - Приховування деталей реалізації обʼєкта від зовнішнього світу.
    - Доступ до даних через публічні методи (геттери/сеттери).
    - ✅ Переваги: контроль над даними, захист від неправильного використання.

2. **Наслідування**
    - Створення нового класу на основі вже існуючого.
    - Новий клас **успадковує** властивості та методи батьківського класу.
    - 🔁 Зменшує дублювання коду, покращує повторне використання.

3. **Поліморфізм**
    - Можливість взаємодіяти з обʼєктами через спільний інтерфейс, навіть якщо вони реалізовані по-різному.
    - Приклад: метод `render()` може бути реалізований по-різному в класах `Button`, `Input`, але викликається однаково.

4. **Абстракція**
    - Виділення лише суттєвих характеристик обʼєкта.
    - Сховати складність, залишити тільки необхідні для користувача деталі.

📦 **Приклад у JavaScript:**
```js
class Animal {
  constructor(name) {
    this.name = name;
  }
  speak() {
    console.log(`${this.name} makes a sound.`);
  }
}

class Dog extends Animal {
  speak() {
    console.log(`${this.name} barks.`);
  }
}

const dog = new Dog('Rex');
dog.speak(); // Rex barks.
```

➡️ В ООП зручно моделювати реальні обʼєкти: кнопки, компоненти, сутності користувачів тощо.

---

### 6. Яка різниця між класом і абстрактним класом?

📌 **Звичайний клас** — це шаблон для створення об'єктів. Його можна безпосередньо інстанціювати (створити об’єкт за допомогою `new`).

📌 **Абстрактний клас** — це концептуальний шаблон, який не можна інстанціювати напряму. Його використовують як основу для інших класів.

**JS-приклад через конструкцію з `throw`:**
```js
class Shape {
  constructor() {
    if (this.constructor === Shape) {
      throw new Error("Abstract class can't be instantiated.");
    }
  }
  draw() {
    throw new Error("Method 'draw()' must be implemented.");
  }
}

class Circle extends Shape {
  draw() {
    console.log("Drawing a circle");
  }
}

const shape = new Circle();
shape.draw(); // Drawing a circle
```

---

### 7. Що таке DOCTYPE і навіщо його використовують?

📄 `<!DOCTYPE html>` — це декларація типу документа, яка потрібна браузеру, щоб правильно відображати HTML-сторінку.

👉 У HTML5 ця декларація є спрощеною і активує **стандартний режим рендерингу**. Без неї браузер може працювати в режимі сумісності, що призводить до непередбачуваного відображення.

---

### 8. Які основні теги структури HTML-сторінки?

🧱 Мінімальний базовий шаблон HTML5:
```html
<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8" />
    <title>Title</title>
  </head>
  <body>
    <header></header>
    <main></main>
    <footer></footer>
  </body>
</html>
```

📌 Основні теги:
- `<!DOCTYPE>` — тип документа
- `<html>` — кореневий елемент
- `<head>` — метаінформація
- `<body>` — вміст сторінки

---

### 9. Що таке семантичні теги та навіщо вони потрібні?

📚 **Семантичні теги** — це теги, які описують зміст елемента:
- `<header>`, `<footer>`, `<article>`, `<section>`, `<nav>`, `<aside>` тощо.

✅ Вони:
- Покращують **SEO**
- Роблять контент доступнішим для screen reader'ів
- Полегшують підтримку коду

📌 Поганий приклад:
```html
<div class="footer"></div>
```

📌 Кращий варіант:
```html
<footer></footer>
```

---

### 10. Що можете сказати про тег `<br>`?

🔸 Тег `<br>` вставляє **розрив рядка** у тексті. Це **самозакривний елемент**, який часто використовують у поезії, адресах або де важливо зберегти структуру тексту.

📌 Приклад:
```html
<p>Місто Київ<br>вул. Хрещатик, 1</p>
```

⚠️ Не слід використовувати `<br>` для створення відступів або оформлення макету — для цього краще застосовувати CSS (`margin`, `padding`).

---

### 11. Які теги з найсвіжіших оновлень ви знаєте?

🔍 HTML постійно еволюціонує, і після HTML5 нові теги та атрибути зʼявляються поступово.

📌 Серед новіших:
- `<template>` — шаблон, який не рендериться одразу, але може використовуватись JavaScript
- `<dialog>` — нативні модальні вікна
- `<slot>` — використовується у Web Components
- `loading="lazy"` — для `<img>` і `<iframe>` — відкладене завантаження
- `enterkeyhint` — мобільний UX (тип поведінки Enter-клавіші)

✅ Використання сучасних тегів і атрибутів покращує UX, продуктивність та підтримує доступність.

---

### 12. У чому різниця між `<div>` і `<span>`?

📦 Обидва — **несемантичні** контейнери, але мають різне відображення:

- `<div>` — **блочний елемент** (`block`)
- `<span>` — **вбудований елемент** (`inline`)

🔍 Приклади використання:
```html
<div>Це блок</div>
<span>А це частина тексту</span>
```

✅ `<div>` — для макету, `<span>` — для стилізації тексту або частин елементів.

---

### 13. Що таке data-атрибути, в яких випадках їх використовуєте?

🏷️ `data-*` — користувацькі атрибути, які дозволяють зберігати дані прямо в HTML.

🔧 Приклад:
```html
<button data-user-id="123">Видалити</button>
```

📦 Доступ у JavaScript:
```js
const id = button.dataset.userId;
```

✅ Зручно для зберігання метаданих без зміни структури DOM. Не призначено для секретних чи великих обсягів даних.

---

### 14. Як використовують атрибути class та id? Яка різниця між класами та ідентифікаторами?

🆔 `id` — унікальний для сторінки, використовується для прив’язки, JS або якорів.

🎯 `class` — багаторазовий, дозволяє групувати елементи для стилізації чи логіки.

📌 CSS селектори:
```css
#main { ... }   /* id */
.item { ... }   /* class */
```

📌 JS:
```js
document.getElementById("main");
document.querySelector(".item");
```

⚠️ Уникайте повторного `id`. Віддавайте перевагу `class` для стилів та взаємодії.

---

### 15. Що таке внутрішні та зовнішні гіперпосилання і які вони мають атрибути?

🔗 **Внутрішні посилання** — в межах сайту або сторінки:
```html
<a href="#contacts">Контакти</a>
```

🌐 **Зовнішні посилання** — на інші сайти:
```html
<a href="https://example.com" target="_blank" rel="noopener noreferrer">Перейти</a>
```

📌 Основні атрибути:
- `href` — куди веде
- `target="_blank"` — відкрити в новій вкладці
- `rel="noopener noreferrer"` — безпека та продуктивність

✅ Завжди додавайте `rel` для безпечного відкриття зовнішніх посилань.

---

### 16. Чим відрізняється тег `<section>` від `<div>`?

📌 `<section>` — **семантичний** тег, а `<div>` — **несемантичний**.

- `<section>` використовується для логічно завершених блоків контенту (наприклад, розділ "Про нас", "Контакти").
- `<div>` — універсальний контейнер без конкретного значення.

🔍 Використовуйте `<section>` лише тоді, коли контент має заголовок або логічну завершеність.

---

### 17. Яка різниця між блочними та вбудованими (inline) елементами?

📦 **Блочні елементи:**
- займають всю ширину батьківського елемента
- починаються з нового рядка
- приклади: `<div>`, `<p>`, `<h1>`, `<section>`

🔠 **Inline елементи:**
- займають лише стільки місця, скільки потрібно контенту
- не починаються з нового рядка
- приклади: `<span>`, `<a>`, `<strong>`, `<em>`

✅ Для побудови макету використовуйте блочні, для форматування тексту — inline.

---

### 18. Що таке псевдокласи в CSS?

🎯 **Псевдокласи** — це ключові слова, що додаються до селектора, щоб вказати його стан.

📌 Приклади:
- `:hover` — при наведенні
- `:focus` — при фокусі (наприклад, інпут)
- `:nth-child(n)` — n-та дитина
- `:first-child`, `:last-child`

```css
button:hover {
  background-color: blue;
}
```

✅ Псевдокласи дозволяють стилізувати динамічні стани елементів без JavaScript.

---

### 19. Що таке псевдоелементи в CSS?

🧩 **Псевдоелементи** — це віртуальні елементи, які не існують в HTML, але можуть бути стилізовані.

📌 Найчастіше використовуються:
- `::before` — вставляє контент перед елементом
- `::after` — після елемента
- `::first-line`, `::first-letter`

```css
p::before {
  content: "👉 ";
}
```

✅ Псевдоелементи зручні для декоративних ефектів та структурних стилів.

---

### 20. У чому різниця між `em` і `strong`?

🔡 Обидва елементи використовуються для **семантичного виділення тексту**.

- `<em>` — **емоційне наголошення** (виділення інтонацією)
- `<strong>` — **важливість** (логічне або структурне наголошення)

📌 За замовчуванням:
- `<em>` — курсив
- `<strong>` — жирний текст

✅ Не використовуйте їх лише для стилів — краще застосовувати `font-style`, `font-weight` у CSS.

---

### 21. Що таке специфічність (specificity) у CSS?

📐 **Специфічність** — це механізм, який визначає, яке саме CSS-правило застосовується до елемента, якщо є конфлікт.

🔢 Пріоритетність (від найменшого до найбільшого):
1. Теги (`div`, `h1`) → 0-0-1
2. Класи, псевдокласи (`.box`, `:hover`) → 0-1-0
3. ID (`#main`) → 1-0-0
4. Inline-стилі (`style="..."`) → 1-0-0-0

📌 Приклад:
```css
p { color: black; }        /* 0-0-1 */
p.intro { color: green; }  /* 0-1-1 */
#main p.intro { color: red; } /* 1-1-1 */
```

✅ `red` переможе, бо має найвищу специфічність.

---

### 22. Які існують способи підключення CSS до HTML?

🔗 Існує три основні способи:

1. **Зовнішній файл**:
```html
<link rel="stylesheet" href="style.css">
```

2. **Усередині `<style>`**:
```html
<style>
  body { background: #eee; }
</style>
```

3. **Inline-стилі**:
```html
<div style="color: red;">Текст</div>
```

✅ Найкраща практика — використовувати зовнішній CSS для підтримуваності.

---

### 23. Що таке модель коробки (box model) у CSS?

📦 **Box Model** — це спосіб, у який браузер розраховує розміри елемента.

🧱 Складається з:
- `content` — вміст
- `padding` — внутрішній відступ
- `border` — рамка
- `margin` — зовнішній відступ

```text
| margin |
  | border |
    | padding |
      | content |
```

📌 Для управління можна використовувати:
```css
box-sizing: border-box;
```

✅ Це дозволяє включати `padding` і `border` у загальну ширину/висоту.

---

### 24. Що таке медіа-запити (media queries)?

📱 **Media Queries** — механізм адаптивної верстки.

📌 Синтаксис:
```css
@media (max-width: 768px) {
  body { font-size: 14px; }
}
```

✅ Дозволяють змінювати стилі під різні пристрої:
- ширина вікна
- орієнтація (landscape/portrait)
- тип пристрою

---

### 25. Як працює flexbox?

🎯 **Flexbox** — це модуль CSS для створення адаптивних макетів одновимірного напрямку (рядок або стовпець).

📌 Основні властивості контейнера:
- `display: flex`
- `flex-direction: row | column`
- `justify-content` — вирівнювання по головній осі
- `align-items` — по перпендикулярній

📌 Для елементів:
- `flex: 1` — зайняти доступний простір
- `order` — порядок відображення

✅ Простий та гнучкий спосіб створювати адаптивні layout'и.

---

### 26. Що таке Grid у CSS і коли його використовують?

🧮 **CSS Grid** — це система двовимірного макету (по рядках і колонках).

📌 Основні властивості контейнера:
```css
display: grid;
grid-template-columns: repeat(3, 1fr);
grid-template-rows: auto;
gap: 10px;
```

📌 Властивості елементів:
```css
grid-column: 1 / 3;
grid-row: 2;
```

✅ Використовуйте Grid, коли потрібно будувати складні макети з обома вимірами (наприклад, таблиці, галереї).

---

### 27. Як зробити адаптивний дизайн?

📱 **Адаптивний дизайн** — це підхід, при якому інтерфейс підлаштовується під розміри екрана.

📌 Методи:
- Медіа-запити (`@media`)
- Відносні одиниці (`%`, `vw`, `vh`, `em`, `rem`)
- Гнучкі сітки (`flexbox`, `grid`)
- Зображення: `srcset`, `sizes`, `picture`
- Мобільне меню: бургер, drawer

✅ Добра практика — дизайн «mobile first».

---

### 28. У чому різниця між `==` і `===` у JavaScript?

🟡 `==` — нестроге порівняння (з перетворенням типів)

🟢 `===` — строге порівняння (тип і значення повинні збігатися)

📌 Приклади:
```js
0 == '0'     // true
0 === '0'    // false
null == undefined // true
null === undefined // false
```

✅ Рекомендується завжди використовувати `===` для уникнення неочікуваних результатів.

---

### 29. Що таке hoisting у JavaScript?

📤 **Hoisting** — механізм, при якому оголошення змінних та функцій піднімаються на початок області видимості.

📌 Приклад:
```js
console.log(x); // undefined
var x = 5;
```

🟡 `var` — піднімається, але ініціалізація ні.

🟢 `let` і `const` — також піднімаються, але не ініціалізуються (потрапляють у т.зв. "Temporal Dead Zone").

✅ Функції, оголошені через `function`, піднімаються повністю.

---

### 30. У чому різниця між var, let і const?

📌 **var**
- функціональна область видимості
- можна перевизначити
- дозволяє оголошення повторно
- hoisting без TDZ

📌 **let**
- блочна область видимості
- можна змінювати значення
- не можна перевизначити в тій самій області
- hoisting з TDZ

📌 **const**
- блочна область видимості
- не можна змінити значення
- потрібна ініціалізація одразу

✅ Рекомендується використовувати `const` за замовчуванням, а `let` — лише коли потрібно змінити значення.

---

### 31. Що таке область видимості (scope)?

📌 **Scope** — це контекст, у якому оголошені змінні доступні.

🧱 Види:
- **Глобальна** — доступна всюди
- **Функціональна** — створюється всередині функцій (`var`)
- **Блочна** — створюється блоками `{}` (`let`, `const`)

📌 Приклад:
```js
let x = 10;
function test() {
  let y = 5;
  console.log(x); // доступно
  console.log(y); // доступно
}
console.log(y); // помилка
```

✅ Використання `let` і `const` дозволяє уникати неочікуваного доступу.

---

### 32. Що таке замикання (closure)?

🔒 **Замикання** — це функція, яка «памʼятає» змінні з обʼєму, в якому була створена, навіть якщо вона виконується за його межами.

📌 Приклад:
```js
function outer() {
  let count = 0;
  return function inner() {
    count++;
    return count;
  }
}

const counter = outer();
counter(); // 1
counter(); // 2
```

✅ Замикання — основа для інкапсуляції та збереження стану в JS.

---

### 33. Що таке this у JavaScript?

🔁 `this` — це ключове слово, яке вказує на **контекст виконання функції**.

📌 Залежить від того, як функція викликана:
- в обʼєкті: `this` = обʼєкт
- у глобальному просторі: `this` = `window` (в браузері)
- у стрілковій функції: `this` береться з зовнішнього контексту

📌 Приклад:
```js
const obj = {
  name: 'JS',
  say() {
    console.log(this.name);
  }
};
obj.say(); // "JS"
```

---

### 34. Що таке event loop?

🔄 **Event Loop** — механізм, що дозволяє JavaScript бути **асинхронним**, незважаючи на однопоточність.

🧱 Черги:
- Call Stack — стек викликів
- Callback Queue — колбек-черга
- Microtask Queue — проміси (виконуються раніше)

📌 Порядок:
1. Виконуються всі синхронні дії
2. Потім мікрозавдання (promise)
3. Потім макрозавдання (setTimeout, event listener)

✅ Розуміння event loop важливе для написання ефективного асинхронного коду.

---

### 35. Як працює setTimeout та setInterval?

🕒 Обидва — функції для **відкладеного або повторюваного** виконання коду.

📌 `setTimeout(fn, delay)` — виконає `fn` один раз через `delay` мс

📌 `setInterval(fn, delay)` — буде виконувати `fn` кожні `delay` мс

📌 Зупинка:
```js
const id = setInterval(() => console.log('tick'), 1000);
clearInterval(id);

const t = setTimeout(() => console.log('done'), 3000);
clearTimeout(t);
```

✅ Не гарантує точного часу — залежить від event loop і навантаження.

---

### 36. Як працює Promise у JavaScript?

📦 **Promise** — це об’єкт, який представляє **асинхронну операцію**, що може завершитися успішно або з помилкою.

📌 Стан:
- `pending` — очікування
- `fulfilled` — виконано
- `rejected` — помилка

📌 Приклад:
```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => resolve("OK"), 1000);
});

promise.then(result => console.log(result));
```

✅ `.then()` — для успіху, `.catch()` — для помилок, `.finally()` — завжди виконується.

---

### 37. Що таке async/await?

⏳ **async/await** — це синтаксичний цукор над Promise, який дозволяє писати асинхронний код як синхронний.

📌 Приклад:
```js
async function getData() {
  try {
    const res = await fetch('/api/data');
    const json = await res.json();
    console.log(json);
  } catch (err) {
    console.error(err);
  }
}
```

✅ `await` зупиняє виконання функції до завершення промісу.

---

### 38. Що таке DOM?

🌐 **DOM (Document Object Model)** — обʼєктне представлення HTML-документа у вигляді дерева вузлів.

📌 Дозволяє:
- отримувати елементи: `getElementById`, `querySelector`
- змінювати структуру, стилі, атрибути
- реагувати на події

```js
const btn = document.querySelector('button');
btn.textContent = "Натисни мене";
```

✅ DOM — основа для взаємодії з користувачем на сторінці.

---

### 39. Як працює addEventListener?

🎯 `addEventListener` — метод для призначення обробника подій.

📌 Синтаксис:
```js
element.addEventListener('click', function(event) {
  console.log('Clicked!', event);
});
```

🧩 Переваги:
- Можна додати кілька обробників
- Можна використовувати `once`, `passive`, `capture` опції
- Легко видалити: `removeEventListener`

---

### 40. Що таке делегування подій?

🔁 **Делегування подій** — це техніка, при якій обробник призначається на батьківський елемент, а не на кожен окремий.

📌 Працює завдяки `event.target`:
```js
document.querySelector('#list').addEventListener('click', (e) => {
  if (e.target.matches('li')) {
    console.log('Клік по елементу списку');
  }
});
```

✅ Ефективно, коли елементи створюються динамічно або їх багато.

---

## 🧠 JavaScript — Події та робота з DOM

### 41. Які події існують у DOM?

📌 Основні типи подій:
- **Мишка:** `click`, `dblclick`, `mousedown`, `mouseup`, `mousemove`
- **Клавіатура:** `keydown`, `keyup`, `keypress`
- **Форма:** `submit`, `change`, `input`, `focus`, `blur`
- **Вікно:** `load`, `resize`, `scroll`, `unload`
- **Drag & Drop:** `dragstart`, `dragover`, `drop`

✅ Для обробки використовують `addEventListener`.

---

### 42. Що таке event.preventDefault()?

🚫 `event.preventDefault()` — метод, який **зупиняє стандартну поведінку браузера**.

📌 Приклади:
- Не відправляти форму:
```js
form.addEventListener('submit', (e) => {
  e.preventDefault();
});
```
- Відключити перехід за посиланням:
```js
a.addEventListener('click', (e) => e.preventDefault());
```

✅ Корисно для кастомної логіки (валидація перед сабмітом тощо).

---

### 43. Що таке event.stopPropagation()?

🛑 `event.stopPropagation()` — зупиняє **поширення події** вгору по DOM-дереву.

📌 Приклад:
```js
document.body.addEventListener('click', () => console.log('body'));
document.querySelector('button').addEventListener('click', (e) => {
  e.stopPropagation();
  console.log('button');
});
```

✅ Корисно, коли не хочемо, щоб подія доходила до батьківських елементів.

---

### 44. Як працює метод querySelector?

🔍 `document.querySelector()` — повертає **перший елемент**, що відповідає CSS-селектору.

📌 Приклади:
```js
document.querySelector('#app');
document.querySelector('.button');
document.querySelector('input[type="text"]');
```

✅ Якщо потрібно кілька елементів — використовуйте `querySelectorAll`.

---

### 45. У чому різниця між innerHTML і textContent?

📌 `innerHTML` — повертає HTML-код всередині елемента:
```js
el.innerHTML = '<b>Привіт</b>';
```

📌 `textContent` — повертає лише текст без HTML:
```js
el.textContent = '<b>Привіт</b>'; // Виведе як текст
```

✅ `textContent` — безпечніше, бо не виконує HTML.

⚠️ `innerHTML` може бути вразливим до XSS, якщо вставляти дані без ескейпінгу.

---

## 🧠 JavaScript — Основи роботи з масивами та обʼєктами

### 46. Які методи масивів ви знаєте?

📌 Основні методи:
- **Ітерація:** `forEach`, `map`, `filter`, `reduce`, `some`, `every`, `find`, `findIndex`
- **Мутуючі:** `push`, `pop`, `shift`, `unshift`, `splice`, `sort`, `reverse`
- **Не мутуючі:** `slice`, `concat`, `flat`, `includes`, `indexOf`, `join`

📌 Приклад:
```js
const arr = [1, 2, 3];
const doubled = arr.map(n => n * 2); // [2, 4, 6]
```

✅ `map` та `filter` не змінюють оригінальний масив.

---

### 47. Як копіювати обʼєкти в JavaScript?

📋 **Поверхневе копіювання:**
- `Object.assign({}, obj)`
- spread оператор: `{ ...obj }`

📋 **Глибоке копіювання:**
- `JSON.parse(JSON.stringify(obj))` — простий спосіб, але без функцій і `undefined`
- `structuredClone(obj)` — сучасний стандарт (підтримка в нових браузерах)

✅ Для складних структур — краще використовувати бібліотеки (`lodash.cloneDeep`).

---

### 48. Що таке деструктуризація?

🧩 **Деструктуризація** — синтаксис для витягування значень з обʼєктів чи масивів.

📌 Масиви:
```js
const [a, b] = [1, 2];
```

📌 Обʼєкти:
```js
const user = { name: "Ann", age: 25 };
const { name } = user;
```

✅ Можна задавати значення за замовчуванням, перейменовувати поля.

---

### 49. Як працює оператор spread (`...`)?

🌍 **Spread** розгортає масив або обʼєкт в окремі елементи.

📌 Масиви:
```js
const nums = [1, 2, 3];
const copy = [...nums]; // копія
```

📌 Обʼєкти:
```js
const obj1 = { a: 1 };
const obj2 = { ...obj1, b: 2 };
```

✅ Зручно для копіювання, обʼєднання, передачі аргументів.

---

### 50. Як працює оператор rest (`...`)?

📥 **Rest** збирає залишкові аргументи в масив.

📌 У функціях:
```js
function sum(...nums) {
  return nums.reduce((a, b) => a + b, 0);
}
```

📌 У деструктуризації:
```js
const [first, ...rest] = [1, 2, 3]; // rest = [2, 3]
```

✅ Rest і spread виглядають однаково (`...`), але працюють по-різному — один «розпаковує», інший «збирає».

---

## 🧠 JavaScript — Поглиблене розуміння

### 51. Що таке замикання (closure) і для чого його використовують?

🔒 Замикання — це функція, яка запамʼятовує змінні з області видимості, в якій вона була створена.

📌 Приклад:
```js
function counter() {
  let count = 0;
  return function() {
    count++;
    return count;
  };
}

const add = counter();
add(); // 1
add(); // 2
```

✅ Зручно для створення приватних змінних і інкапсуляції логіки.

---

### 52. Що таке IIFE (Immediately Invoked Function Expression)?

⚡ IIFE — це функція, яка викликається одразу після оголошення.

📌 Синтаксис:
```js
(function() {
  console.log("Виконано одразу!");
})();
```

✅ Використовується для створення ізольованої області видимості.

---

### 53. Яка різниця між call, apply та bind?

🔧 Усі три методи дозволяють викликати функцію з вказаним `this`.

📌 `call` — передає аргументи по одному:
```js
func.call(context, arg1, arg2);
```

📌 `apply` — передає аргументи масивом:
```js
func.apply(context, [arg1, arg2]);
```

📌 `bind` — повертає нову функцію з привʼязаним контекстом:
```js
const boundFunc = func.bind(context);
boundFunc();
```

---

### 54. Що таке прототипне наслідування?

🧬 У JavaScript кожен обʼєкт має прихований `[[Prototype]]`, через який відбувається наслідування.

📌 Приклад:
```js
const animal = { eats: true };
const dog = Object.create(animal);
console.log(dog.eats); // true
```

✅ Прототип — це обʼєкт, з якого інші обʼєкти можуть "успадковувати" властивості.

❗ Спеціальний випадок: Null-прототип
```js
const obj = Object.create(null);
console.log(obj.toString); // undefined
```
Створення “чистого” об’єкта без зайвих властивостей — корисно для мап (dictionary-like).

---

### 55. У чому різниця між класами та прототипами?

📌 **Класи** — синтаксичний цукор над прототипами:
```js
class Person {
  constructor(name) {
    this.name = name;
  }
  greet() {
    console.log("Hi,", this.name);
  }
}
```

📌 **Прототипи** — основа механізму наслідування:
```js
function Person(name) {
  this.name = name;
}
Person.prototype.greet = function() {
  console.log("Hi,", this.name);
};
```

✅ Класи читаються краще, але під капотом працюють через прототипи.

---

## 🧠 JavaScript — Поглиблена робота з обʼєктами та класами

### 56. Як працює оператор instanceof?

🔍 `instanceof` перевіряє, чи є обʼєкт екземпляром певного класу або конструктора.

📌 Приклад:
```js
function Animal() {}
const dog = new Animal();

console.log(dog instanceof Animal); // true
console.log(dog instanceof Object); // true
```

✅ Працює через перевірку ланцюжка прототипів.

---

### 57. Як працює Object.create()?

🛠️ `Object.create(proto)` створює новий обʼєкт із вказаним прототипом.

📌 Приклад:
```js
const animal = { eats: true };
const dog = Object.create(animal);

console.log(dog.eats); // true
```

✅ Утворює чисте прототипне наслідування без виклику конструктора.

---

### 58. Що таке геттер і сеттер?

🧩 **Getter** і **Setter** — це спеціальні методи для читання/запису властивостей обʼєкта.

📌 Приклад:
```js
const user = {
  firstName: "Ann",
  lastName: "Smith",
  get fullName() {
    return this.firstName + " " + this.lastName;
  },
  set fullName(value) {
    [this.firstName, this.lastName] = value.split(" ");
  }
};

console.log(user.fullName); // "Ann Smith"
user.fullName = "John Doe";
console.log(user.firstName); // "John"
```

✅ Дає контроль над доступом до властивостей.

---

### 59. Як працює оператор optional chaining (`?.`)?

🔗 **Optional chaining** дозволяє безпечно звертатися до вкладених властивостей, навіть якщо деякі з них `undefined` або `null`.

📌 Приклад:
```js
const user = {};
console.log(user?.address?.street); // undefined
```

✅ Уникає помилок `Cannot read property of undefined`.

---

### 60. Що таке nullish coalescing (`??`)?

🟢 **Nullish coalescing** повертає значення справа, якщо зліва `null` або `undefined`.

📌 Приклад:
```js
let user;
console.log(user ?? "Анонім"); // "Анонім"
```

📌 Відмінність від `||`: `??` **не реагує** на `0`, `''`, `false`.

✅ Зручно задавати значення за замовчуванням тільки при відсутності даних.

---

## ⚙️ JavaScript — Робота з JSON, модулі, інструменти

### 61. Що таке JSON і як з ним працювати?

📦 **JSON (JavaScript Object Notation)** — формат обміну даними, схожий на обʼєкти JavaScript.

📌 Серіалізація:
```js
const user = { name: "Ivan", age: 30 };
const json = JSON.stringify(user); // '{"name":"Ivan","age":30}'
```

📌 Десеріалізація:
```js
const obj = JSON.parse(json);
```

✅ Використовується в API, збереженні даних, локальному сховищі.

---

### 62. Як працює localStorage?

💾 `localStorage` — API браузера для зберігання пар ключ–значення **на невизначений термін**.

📌 Приклад:
```js
localStorage.setItem('token', '123abc');
const token = localStorage.getItem('token');
localStorage.removeItem('token');
localStorage.clear(); // очищає все
```

✅ Дані не стираються при оновленні сторінки.

---

### 63. Що таке модулі в JavaScript?

📦 Модулі — це окремі файли з кодом, які можна імпортувати або експортувати.

📌 `export`:
```js
export const name = "Ann";
export default function() {}
```

📌 `import`:
```js
import { name } from './user.js';
import myFunc from './file.js';
```

✅ Дозволяють ізолювати логіку та повторно використовувати код.

---

### 64. Як працює import/export?

🔄 `import` / `export` — ключові слова для організації коду у модулях.

📌 Іменований експорт:
```js
export const greet = () => {};
import { greet } from './utils.js';
```

📌 Експорт за замовчуванням:
```js
export default function() {};
import myFunc from './utils.js';
```

✅ Один файл може мати лише один `export default`, але багато `export`.

---

### 65. Що таке Babel?

🛠️ **Babel** — JavaScript-компілятор, який перетворює сучасний код (ES6+) у код, сумісний зі старими браузерами.

📌 Приклад:
```js
// Вихідний код
const sum = (a, b) => a + b;

// Babel-трансформований код
var sum = function(a, b) {
  return a + b;
};
```

✅ Часто використовується з Webpack, React, TypeScript для підтримки сумісності.

---

## ⚙️ JavaScript — Інструменти, дебагінг, продуктивність

### 66. Що таке Webpack?

📦 **Webpack** — це модульний збирач (module bundler) для JavaScript-додатків.

📌 Основні можливості:
- збірка JS, CSS, зображень
- підтримка `loaders` для трансформації файлів
- `plugins` для оптимізації (minify, caching тощо)
- підтримка hot-reload у dev-сервері

✅ Полегшує керування залежностями та оптимізує продуктивність додатків.

---

### 67. Що таке Source Maps?

🗺️ **Source Map** — це файл, який дозволяє браузеру повʼязати скомпільований (мінімізований) код із вихідним.

📌 Дає можливість:
- дебажити код у DevTools як ніби він не був змінений
- знаходити помилки у вихідному коді

✅ У Webpack додається через `devtool: 'source-map'`.

---

### 68. Як дебажити JavaScript-код?

🛠️ Основні способи:
- `console.log()`, `console.table()`
- `debugger` — зупиняє виконання, відкриває DevTools
- Breakpoints у Chrome DevTools
- Вкладка Sources → Call stack, Scope, Watch

✅ Краще уникати `console.log` у production-коді.

---

### 69. Що таке throttling та debouncing?

⏳ Обидва — техніки оптимізації викликів функцій, які виконуються надто часто (наприклад, scroll, resize, input).

📌 **Debounce** — викликає функцію **після завершення** серії подій:
```js
function debounce(fn, delay) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}
```

📌 **Throttle** — викликає функцію **не частіше ніж** вказаний інтервал:
```js
function throttle(fn, limit) {
  let inThrottle;
  return function(...args) {
    if (!inThrottle) {
      fn.apply(this, args);
      inThrottle = true;
      setTimeout(() => (inThrottle = false), limit);
    }
  };
}
```

---

### 70. Як підвищити продуктивність JavaScript-додатка?

🚀 Основні підходи:
- Використовувати **code splitting**, `lazy loading`
- Оптимізувати **рендеринг** (мемоізація, virtual DOM)
- Зменшити розмір bundle (Tree Shaking, minification)
- Використовувати **debounce/throttle** для подій
- Кешування даних (в памʼяті або через Service Workers)
- Уникати важких циклів та блокувань потоку (Web Workers для важких обчислень)

✅ Також важливо стежити за Lighthouse/Performance вкладками у DevTools.

---

## 🧪 JavaScript — Тестування, безпека, оптимізація

### 71. Що таке юніт-тести?

🧪 **Юніт-тест** — це автоматичний тест, який перевіряє окрему функцію або компонент у відриві від інших частин системи.

📌 Переваги:
- швидкий зворотний звʼязок
- попередження помилок
- впевненість при рефакторингу

✅ Популярні фреймворки: Jest, Mocha, Vitest.

---

### 72. Що таке тестове покриття (test coverage)?

📊 **Test coverage** — це метрика, яка показує, яку частину коду охоплюють тести.

📌 Види покриття:
- statements (вирази)
- branches (розгалуження)
- functions (функції)
- lines (рядки)

✅ Використовується для визначення «сліпих зон» у тестах.

---

### 73. Що таке XSS і як його уникнути?

🔐 **XSS (Cross-Site Scripting)** — атака, при якій зловмисник вставляє шкідливий JavaScript у сторінку.

📌 Наприклад:
```js
element.innerHTML = userInput; // небезпечно!
```

✅ Як уникнути:
- використовувати `textContent` замість `innerHTML`
- ескейпити вхідні дані
- встановити **Content Security Policy (CSP)**

---

### 74. Що таке CSP?

🛡️ **Content Security Policy (CSP)** — це HTTP-заголовок, який обмежує джерела скриптів, стилів, зображень тощо.

📌 Приклад:
```
Content-Security-Policy: default-src 'self'; script-src 'self' https://trusted.cdn.com
```

✅ Захищає від XSS та інших атак на завантаження несанкціонованого коду.

---

### 75. Як покращити безпеку фронтенду?

🔐 Основні практики:
- Уникати `innerHTML`, використовувати `textContent`
- Перевіряти та валідовати введення користувача
- Встановлювати CSP
- Оновлювати залежності (npm audit)
- Використовувати HTTPS
- Захист від clickjacking: `X-Frame-Options: DENY`

✅ Безпека — це процес, а не одноразове рішення.

---

## 📚 HTML/CSS — Форматування, доступність, UX

### 76. Що таке семантична верстка?

🧠 **Семантична верстка** — використання HTML-тегів згідно з їхнім змістовим призначенням.

📌 Приклад:
- `header`, `nav`, `article`, `section`, `footer` замість `div`

✅ Переваги:
- краща доступність (screen readers)
- SEO-оптимізація
- зрозуміліша структура коду

---

### 77. Що таке ARIA-атрибути?

♿ **ARIA (Accessible Rich Internet Applications)** — набір атрибутів для покращення доступності інтерфейсів.

📌 Приклади:
- `role="button"`
- `aria-label="Закрити"`
- `aria-hidden="true"`

✅ Дає змогу допоміжним технологіям (наприклад, VoiceOver) краще інтерпретувати інтерфейс.

---

### 78. Як зробити сайт доступним для всіх користувачів?

🧩 Основи доступності (a11y):
- використовувати семантичну верстку
- контрастність тексту
- фокусування елементів (`tabindex`, `:focus`)
- текстові альтернативи для зображень (`alt`)
- ARIA-атрибути
- адаптація до клавіатури

✅ Також існують тести: Lighthouse, axe, screen readers.

---

### 79. Що таке responsive design?

📱 **Responsive design** — підхід до створення інтерфейсів, які адаптуються під різні розміри екранів.

📌 Техніки:
- медіа-запити (`@media`)
- відсоткові/гнучкі ширини
- flex/grid layout
- мобільне меню, адаптивна типографіка

✅ UX повинен залишатись зручним на будь-якому пристрої.

---

### 80. Що таке progressive enhancement?

🌱 **Progressive Enhancement** — стратегія розробки, за якої базовий функціонал доступний усім користувачам, а додатковий — тільки за підтримки нових технологій.

📌 Приклад:
- просте посилання → JavaScript додає модальне вікно
- `form` працює без JS → з JS додається валідація

✅ Це гарантує доступність і кращий досвід на старих пристроях або при поганому зʼєднанні.

